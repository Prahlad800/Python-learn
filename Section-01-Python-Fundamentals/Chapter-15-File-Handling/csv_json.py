# Topic: CSV and JSON Files
# Explanation: Python can work with CSV and JSON data using built-in modules.

# Syntax:
# import json
data = {"name": "Asha", "age": 20}
print(json.dumps(data))

# Examples:
# import json
data = {"name": "Asha", "age": 20}
print(json.dumps(data))

# Practice Programs:
# 1. Create a JSON object.
2. Read a CSV file structure.

# Interview Questions:
# Q: Why are CSV and JSON important?
A: They are common formats for data exchange.

# Expected Output:
# {"name": "Asha", "age": 20}

import json
data = {"name": "Asha", "age": 20}
print(json.dumps(data))
