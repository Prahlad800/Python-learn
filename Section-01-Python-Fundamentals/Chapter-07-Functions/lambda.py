# Topic: Lambda Functions
# Explanation: Lambda functions are small anonymous functions.

# Syntax:
# square = lambda x: x * x
print(square(4))

# Examples:
# square = lambda x: x * x
print(square(4))

# Practice Programs:
# 1. Create a lambda that adds two numbers.
2. Create a lambda for checking even numbers.

# Interview Questions:
# Q: When are lambda functions useful?
A: For short, simple operations.

# Expected Output:
# 16

square = lambda x: x * x
print(square(4))
