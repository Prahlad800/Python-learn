# Topic: Tuples
# Explanation: Tuples are immutable sequences.

# Syntax:
# point = (3, 4)
print(point[0])

# Examples:
# point = (3, 4)
print(point)
print(point[0])

# Practice Programs:
# 1. Create a tuple of three items.
2. Access its second element.

# Interview Questions:
# Q: Why use tuples instead of lists?
A: They are immutable and safer for fixed data.

# Expected Output:
# (3, 4)
3

point = (3, 4)
print(point)
print(point[0])
