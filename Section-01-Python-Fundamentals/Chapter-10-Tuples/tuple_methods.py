# Topic: Tuple Methods
# Explanation: Tuples support count() and index().

# Syntax:
# numbers = (1, 2, 2, 3)
print(numbers.count(2))

# Examples:
# numbers = (1, 2, 2, 3)
print(numbers.count(2))
print(numbers.index(3))

# Practice Programs:
# 1. Count how many times a value appears.
2. Find the index of a value.

# Interview Questions:
# Q: Can tuples be modified?
A: No, they are immutable.

# Expected Output:
# 2
3

numbers = (1, 2, 2, 3)
print(numbers.count(2))
print(numbers.index(3))
